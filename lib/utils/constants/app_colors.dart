import 'package:flutter/material.dart';

class AppColors {
  // Arkaplan rengi (#262624)
  static const Color background = Color(0xFF262624);

  // Kutucukların arkaplan rengi (#30302e)
  static const Color surface = Color(0xFF30302E);

  // Vurgu rengi (Accent/Primary) (#db7273)
  static const Color accent = Color(0xFFDB7273);

  // Alt metin rengi (Secondary Text) (#aba8a1)
  static const Color secondaryText = Color(0xFFABA8A1);

  // Ana metin rengi (#ebeae6)
  static const Color text = Color(0xFFEBEAE6);
}